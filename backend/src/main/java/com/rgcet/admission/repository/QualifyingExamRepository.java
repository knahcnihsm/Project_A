package com.rgcet.admission.repository;

import com.rgcet.admission.entity.QualifyingExam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QualifyingExamRepository extends JpaRepository<QualifyingExam, Long> {
}
