package com.rgcet.admission.repository;

import com.rgcet.admission.entity.DiplomaDetails;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DiplomaDetailsRepository extends JpaRepository<DiplomaDetails, Long> {
}
